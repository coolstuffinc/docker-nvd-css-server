server {
	listen 80 default_server;
	listen [::]:80 default_server;

	root /home/steam/htdocs;
	autoindex on; # Helpful to verify files exist

	server_name _;

	location / {
		try_files $uri $uri/ =404;
	}
}
